# API Configuration
# Set to 'true' to use custom API backend (Cloudflare Workers)
# Set to 'false' to use direct API (Sansekai)
VITE_USE_CUSTOM_API=false

# Custom API URL (only used if VITE_USE_CUSTOM_API=true)
# Replace YOUR_SUBDOMAIN with your actual Cloudflare Workers subdomain
VITE_API_URL=https://ctrxl-dracin-api.YOUR_SUBDOMAIN.workers.dev
